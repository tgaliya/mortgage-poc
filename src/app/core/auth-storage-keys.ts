/** Shared sessionStorage key for the logged-in user's id, used by AuthService and
 *  (directly, to avoid a circular dependency on AuthService) UserService's activity logging. */
export const CURRENT_USER_ID_KEY = 'mortgage_poc_current_user_id';
